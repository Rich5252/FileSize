Fast Windows Folder and File Size scanner that scans a given root folder or drive and displays a tree view of the folders with their sizes. Any folder can be clicked to show its list of files in a separate ListView control.

The results are updated dynamically during the scan and can handle hundreds of thousands of folders and millians of files so suitable for analysis of large disc drives.

A Windows Forms app written in C# using VS2026.
